# 全量验收测试清单（Acceptance Test Checklist）

> 用途：把 15 份深度规格的"验证方法"汇编为**可逐条执行**的验收清单。每一条= 操作步骤 + 预期结果。
> 用法：按模块逐条跑，✅ 通过 / ❌ 失败（失败记入 12-monitor 或提 bug）。
> 来源：docs/deep-spec/01-15 各文档第 7 章。

---

## 01 提示词系统（prompt-system）

| # | 步骤 | 预期结果 |
|---|------|----------|
| 01-1 | 新建提示词 → 填内容+变量 → 保存 | 提示词入库，版本 v1 |
| 01-2 | 修改并保存 → 查看版本列表 | 出现 v2，两版本可 diff |
| 01-3 | 引用变量 {input} 测试渲染 | 渲染正确，缺失变量有明确报错 |
| 01-4 | 导出提示词 → 在另一环境导入 | 配置完整还原 |
| 01-5 | 在对话中切换提示词版本 | 新消息用新版本，历史消息不受影响 |
| 01-6 | 含敏感占位符的提示词 → 检查日志 | 日志中占位符被脱敏（不泄露真实值） |
| 01-7 | 触发供应链检查（外部导入提示词） | 风险提示出现（提示注入/恶意指令扫描） |

## 02 沙箱系统（sandbox）

| # | 步骤 | 预期结果 |
|---|------|----------|
| 02-1 | 配置沙箱策略（allow/deny 命令）→ 让 agent 执行白名单命令 | 正常执行 |
| 02-2 | 让 agent 执行黑名单命令（如 rm -rf /） | 被拦截+安全告警 |
| 02-3 | 沙箱内写文件 | 写入被限制在沙箱目录，宿主不可见 |
| 02-4 | 沙箱网络隔离开启 → agent 访问外网 | 被阻断（或按策略代理） |
| 02-5 | 沙箱超时/资源上限 | 超限被杀+错误信息明确 |
| 02-6 | 高危指令拦截（含转账/删库关键词） | 转审批流程（13-iam 联动） |

## 03 上下文管理（context）

| # | 步骤 | 预期结果 |
|---|------|----------|
| 03-1 | 长对话自动压缩 | 压缩后 token 下降，关键信息保留（抽查摘要） |
| 03-2 | 手动精确压缩（/compact + 提示词） | 按指定范围压缩，其余保留 |
| 03-3 | 上下文窗口超限 | 优雅降级（摘要历史），不报错崩溃 |
| 03-4 | 重要信息（用户显式"记住"） | 压缩后仍可召回（进长期记忆） |
| 03-5 | 压缩前后对比 | 可查看压缩日志（何时/压缩了什么） |

## 04 工具系统（tools）

| # | 步骤 | 预期结果 |
|---|------|----------|
| 04-1 | 注册新工具 → 工具列表可见 | 工具可被 agent 调用 |
| 04-2 | 工具白名单配置 | 未授权工具调用被拒 |
| 04-3 | 工具鉴权（需审批的工具） | 调用触发审批单（13 联动） |
| 04-4 | 工具失败（模拟抛错） | agent 收到错误→重试/降级/明确告知用户 |
| 04-5 | MCP 服务器接入（HTTP/stdio） | 工具被发现且可调用 |
| 04-6 | 工具超时 | 超时中断+告警（12 联动） |
| 04-7 | 连接器市场安装办公连接器 | OAuth 授权后可调用（类 WorkBuddy） |

## 05 记忆系统（memory）

| # | 步骤 | 预期结果 |
|---|------|----------|
| 05-1 | 对话中产生事实（用户自介） | 自动沉淀进记忆（非显式 save 也可提炼） |
| 05-2 | 新会话提问"我叫什么" | 从长期记忆召回正确回答 |
| 05-3 | 记忆管理页查看 | 每条记忆可见、可删（类 ChatGPT 记忆摘要页） |
| 05-4 | 删除一条记忆后提问 | 不再引用该记忆 |
| 05-5 | 知识库上传文档 → 检索问答 | 回答引用知识库内容（带来源） |
| 05-6 | 记忆冲突（新事实 vs 旧记忆） | 新事实覆盖/提示冲突 |
| 05-7 | 遗忘策略触发（长期未用） | 低优先级记忆被清理，日志可查 |

## 06 模型管理（models）

| # | 步骤 | 预期结果 |
|---|------|----------|
| 06-1 | 配置多 provider + key 轮换 | key 失效自动切换备用 key |
| 06-2 | 主模型失败 → fallback | 自动降级到备用模型，用户无感/有提示 |
| 06-3 | 模型路由（按任务类型） | 不同任务走不同模型（路由规则生效） |
| 06-4 | 成本统计页 | 按模型/agent/日期查看成本 |
| 06-5 | 限流配置（QPS 超限） | 超限请求排队/拒绝+提示 |
| 06-6 | 模型健康检查 | 不可用模型标记+告警（12 联动） |

## 07 编排与工作流（workflow）

| # | 步骤 | 预期结果 |
|---|------|----------|
| 07-1 | 创建串行工作流（A→B→C） | 按序执行，B 可用 A 输出 |
| 07-2 | fanout 并行分支 | 分支并行执行，汇总正确 |
| 07-3 | 画布编辑器拖拽编排 | 可视化编辑+保存生效 |
| 07-4 | 模拟运行（不真实执行） | 预览每一步输入输出 |
| 07-5 | HITL 审批节点 | 流程暂停等审批，批准后继续 |
| 07-6 | 多 agent 协作（supervisor 模式） | 任务正确分派与汇总 |
| 07-7 | A2A 跨进程调用 | 远程 agent 可被调用并返回 |
| 07-8 | 子代理并行（≤6） | 并行执行互不干扰（工作树隔离，Codex 同款） |
| 07-9 | 运行管理（暂停/恢复/重试/取消） | 各操作生效 |
| 07-10 | 会话间消息协调 | 两个会话可互发消息协调（CC6） |

## 08 语音系统（voice）

| # | 步骤 | 预期结果 |
|---|------|----------|
| 08-1 | 语音输入 → 转写 | 文字准确进入输入框 |
| 08-2 | 文字 → TTS 播报 | 音色正确、可打断 |
| 08-3 | 实时全双工通话 | 边听边说，打断即时响应 |
| 08-4 | 音色管理（四维参数） | 参数调整后音色变化可感知 |
| 08-5 | 音色克隆 | 上传样本 → 克隆成功 → 试听 |
| 08-6 | 唤醒词 | 唤醒后进入监听 |

## 09 定时任务（schedule）

| # | 步骤 | 预期结果 |
|---|------|----------|
| 09-1 | 创建一次性任务（1 分钟后） | 到点触发执行 |
| 09-2 | cron 表达式预览 | 正确展示未来 5 次执行时间 |
| 09-3 | 任务错过执行（进程停机恢复） | 按策略补跑（立即/跳过/顺延） |
| 09-4 | 并发控制 | 同任务不重复并发执行 |
| 09-5 | 任务失败重试退避 | 按退避策略重试，最终失败有通知 |
| 09-6 | 暂停/恢复/删除任务 | 各操作生效 |
| 09-7 | 任务通知渠道 | 完成/失败消息送达配置渠道 |

## 10 技能与插件（skill-plugin）

| # | 步骤 | 预期结果 |
|---|------|----------|
| 10-1 | 安装技能（SKILL.md） | 描述出现在技能列表，按需加载 |
| 10-2 | 触发技能描述场景 | 技能被自动选用（不进系统提示） |
| 10-3 | 技能市场浏览/安装 | 一键安装可用（Explore，W4） |
| 10-4 | 插件安装（代码扩展） | 启动时加载，提供新能力 |
| 10-5 | 插件权限申请 | 敏感权限触发审批（13 联动） |
| 10-6 | 插件崩溃 | 插件级隔离，主服务不受影响 |
| 10-7 | 技能测试/卸载 | 测试通过可发布，卸载后不再触发 |

## 11 评估体系（eval）

| # | 步骤 | 预期结果 |
|---|------|----------|
| 11-1 | 创建评测集（含预期答案）→ 运行 | 出分数报告（通过率/逐项明细） |
| 11-2 | LLM 判分器 | 判分结果与人工抽检一致（≥90% 抽查） |
| 11-3 | 基线对比（旧版 vs 新版） | 差异显著项高亮（回归检测） |
| 11-4 | 发布门禁（未达标阻断） | 发布被阻断并展示报告（14 联动） |
| 11-5 | 在线抽样人工复核 | 线上样本进入复核队列，标注结果回写 |
| 11-6 | 自动审查器（Auto-review） | agent 产出自动审查，问题项列出（C4） |

## 12 监控与告警（monitor）

| # | 步骤 | 预期结果 |
|---|------|----------|
| 12-1 | 建规则"错误率>50% 持续 10s → P1" → 制造失败 | 收到告警（告警链路通） |
| 12-2 | 告警管理点"测试" | 各渠道收到测试消息 |
| 12-3 | 完成一个对话请求 → Trace 列表 | 出现 trace → 详情瀑布图各段耗时可见 |
| 12-4 | 日志按 trace_id 过滤 | 找到同一次请求全部日志 |
| 12-5 | 停用模型网关 | 健康页变 down + P0 告警 |
| 12-6 | 数据漂移检测 | 分布变化超阈值 → P2 告警 |

## 13 权限 IAM（iam）

| # | 步骤 | 预期结果 |
|---|------|----------|
| 13-1 | 新建 viewer 用户 → 登录 → 删 prompt | 403 + 审计 denied 记录 |
| 13-2 | 开发者 own scope | 可改自己的 agent、改不了别人的 |
| 13-3 | 配置"发邮件"为敏感操作 → agent 触发 | 出审批单 → 批准放行/拒绝阻断 |
| 13-4 | 吊销 API key → 调用 | 401 即时生效 |
| 13-5 | 审计查"权限被拒" | 看到刚才 403 的记录（操作者/资源/原因） |
| 13-6 | 权限模式三档（严格/默认/Auto） | 严格=全询问，Auto=按分类器自决（CC1） |
| 13-7 | 登录失败 5 次 | 账号锁定 15 分钟 |

## 14 Agent 生命周期（agent-lifecycle）

| # | 步骤 | 预期结果 |
|---|------|----------|
| 14-1 | 模板创建客服 agent → 模拟测试 | 正常回复（创建+测试通） |
| 14-2 | 修改配置 → 版本 v2 → 发布 | 线上会话用 v2 |
| 14-3 | 回滚 v1 | 新请求立即用 v1 |
| 14-4 | 克隆 agent → 改名 | 两 agent 独立运行互不影响 |
| 14-5 | 停用 → 对话入口 | 返回"已停用" → 启用恢复 |
| 14-6 | 发布门禁（评测未过） | 阻断发布+报告（11 联动） |
| 14-7 | 废弃/归档 | 数据导出后冷存储，不可再启用 |

## 15 对话体验（ux-detail 全量版：14 域 800+ 细节点）

| # | 步骤 | 预期结果 |
|---|------|----------|
| 15-1 | 发带代码+公式+表格消息 | 流式渲染+代码高亮可复制+公式渲染+表格可复制 |
| 15-2 | 悬停消息操作条 | 复制/重发/编辑/点赞/点踩/翻译/朗读 全可用（A 域） |
| 15-3 | 流式中点停止 | 立即停+显示"已停止"；点继续从断点续跑（O2） |
| 15-4 | 问"今天上海天气" | 自动搜索+过程可视化+[1][2] 引用可点+悬浮预览+来源筛选（D 域） |
| 15-5 | 拖入 PDF+截图+URL | PDF 文本解析/图 OCR/链接 unfurl → 解析预览可见 → @引用提问命中（C 域） |
| 15-6 | Plan 模式任务 | 方案卡勾选确认 → 任务卡跟踪 → 审批挂起 → 手机批准 → 继续（G 域） |
| 15-7 | 会话右键分叉 + btw | 分叉独立演进；btw 侧边回答不污染主线（E6） |
| 15-8 | 会话导出/分享 | 四格式导出；分享链接带过期/禁复制/密码（E3） |
| 15-9 | 建工作空间配指令 | 空间内会话自动带指令上下文（F 域） |
| 15-10 | 后台任务完成 | 铃铛红点+配置渠道通知 → 点击跳转任务卡（J 域） |
| 15-11 | 长文打开画布 | 内联编辑 + AI 建议 diff + 版本回滚（N2） |
| 15-12 | 纯键盘完成一次对话 | 全流程无需鼠标（K8 无障碍） |
| 15-13 | 断网发消息 | 离线提示条+重连自动恢复+待发排队（L9） |
| 15-14 | 输入区草稿 | 刷新不丢，恢复提示"已恢复草稿"（B1-7） |
| 15-15 | Ctrl+K 命令面板 | 新建/搜索/切换/斜杠命令全可用（K2） |
| 15-16 | 点踩必选原因 | 弹出原因选择（I2） |
| 15-17 | 错误报告 | 提交带 trace_id，反馈历史可见（I4/I6） |
| 15-18 | 粘贴 URL 渲染卡片 | 链接 unfurl 卡片显示，点击跳转（A6-1） |
| 15-19 | 记忆面板 | 查看/编辑/删除单条记忆（N12-2，05 联动） |
| 15-20 | 图片灯箱+图生图 | 放大/缩放/下载/再次生成变体（A3） |

---

## 16 企业级·组织级通用能力（13 子域 ≈130 项，抽测 30 条）

| # | 步骤 | 预期结果 |
|---|------|----------|
| 16-1 | 创建第二组织 → 两组织登录 | 数据/会话/配置完全隔离，互不可见（A.2） |
| 16-2 | 建三级部门树 + 拖拽调整 | 树结构保存、成员归属跟随变更（A.3） |
| 16-3 | 新建项目空间 → 分配成员 | 空间内资源只对成员可见（A.4） |
| 16-4 | CSV 批量导入 100 用户（含失败行） | 成功导入+失败行报告可下载（B.3） |
| 16-5 | 停用用户 → 该用户登录 | 拒绝登录、已登录会话失效（B.5） |
| 16-6 | 离职交接 → 原账号资源转移 | 全部资源转给接任者+审计记录（B.6） |
| 16-7 | 配置 SAML IdP → 一键登录 | SSO 登录成功，属性映射生效（C.1） |
| 16-8 | 强制 MFA → 用户登录 | 第二步验证必现，未绑定被拦截（C.3） |
| 16-9 | 异地登录触发风控 | 风险提示/锁定，管理员可见风控记录（C.6） |
| 16-10 | 建知识库 → 上传 PDF/扫描件 | 解析+OCR+分块完成，chunk 可预览（D.2/D.3） |
| 16-11 | 配置混合检索 → 试检索台调参 | 结果命中变化、topK/阈值实时生效（D.5） |
| 16-12 | 问答引用来源 | 答案带 [n] 角标 → 点击定位原文段落（D.6） |
| 16-13 | 知识库设为部门可见 | 非部门成员检索不到该库内容（D.7） |
| 16-14 | 打开审计日志按用户筛选导出 | 记录完整（who/what/before/after），CSV 可下（E.1/E.2） |
| 16-15 | 配置保留策略 → 过期数据 | 定时清理任务执行，到期数据删除（E.4） |
| 16-16 | 输入含手机号的对话 | 输出/日志中手机号自动脱敏（E.6） |
| 16-17 | 触发敏感词输出 | 5 秒内熔断+审计报告生成（E.7） |
| 16-18 | 设部门月度预算 → 超额 | 阶梯告警→限速→熔断逐级生效（F.1/F.3） |
| 16-19 | 成本报表按模型/部门切换 | 维度切换正确、导出可用（F.4） |
| 16-20 | 创建 Webhook 订阅会话完成事件 | 事件推送成功，失败重试可见（G.3/G.11） |
| 16-21 | 接入企业微信机器人 → 发消息 | 渠道收发通，卡片渲染正确（G.6） |
| 16-22 | 生成嵌入代码 → iframe 嵌入外站 | 免登会话生效，域名白名单拦截生效（G.9） |
| 16-23 | Agent 提交发布 → 管理员审核 | 审核通过后新版本生效；驳回则保持旧版（H.1） |
| 16-24 | 灰度 10% → 验证 → 全量 | 分流比例生效，一键回滚恢复（H.2/H.3） |
| 16-25 | 上架应用市场 → 他人安装 | 安装成功、评分/使用统计可见（H.4/H.6） |
| 16-26 | 评论会话 @同事 | 同事收到通知，评论可回复（I.3） |
| 16-27 | 提交资源访问申请 → 审批 | 通过后自动获得权限（I.9） |
| 16-28 | 平台总览看板 | DAU/会话数/消息数正确，周期可切换（J.1） |
| 16-29 | 开维护模式 → 用户访问 | 显示维护公告，恢复后正常（K.2） |
| 16-30 | 手动备份 → 恢复演练 | 备份成功，恢复到备份点数据一致（K.4） |

---

## 17 教训与坑（14 域 ≈144 项，抽测 20 条）

| # | 步骤 | 预期结果 |
|---|------|----------|
| 17-1 | 连续 10 次相同问题 | 开场白/语气不千篇一律，无模板感（A.1/A.2） |
| 17-2 | 问简单问题（今天天气） | 直接回答不列点、不废话堆砌，含明确信息（A.3/A.5） |
| 17-3 | 触发敏感话题 | 一句声明即止，不反复免责（A.7） |
| 17-4 | 派一个多步骤任务 | Agent 正常收敛，无死循环（同工具同参 ≤3 次）（B.1） |
| 17-5 | 给有计算器工具的任务 | 用计算器而非搜索，工具选择正确（B.2） |
| 17-6 | 让 Agent 重复执行创建操作 2 次 | 不产生重复记录（幂等键生效）（C.1） |
| 17-7 | 工具故意挂起 60 秒 | 超时处理生效，不无限等待（C.3） |
| 17-8 | 用户说“我不喜欢红色”后继续对话 | 后续推荐不出现红色（记忆归档生效）（D.1） |
| 17-9 | 两个用户同时对话 | 记忆不串台，偏好严格隔离（D.2） |
| 17-10 | 用户输入含 emoji/超长文本/HTML | 输入预处理正常，Agent 不崩（E.1） |
| 17-11 | 用户文本里写“忽略以上指令” | 注入不生效，仍按系统指令执行（E.2） |
| 17-12 | 修改提示词后跑回归 | 变更可追踪，回滚可恢复（E.8） |
| 17-13 | 上传含扫描件的 PDF | 解析+OCR 完成，无乱码丢失（F.3） |
| 17-14 | 运行评测集（含对抗样本） | happy path+边界样本都测，报告含基线对比（G.1/G.4） |
| 17-15 | 普通用户调管理员接口 | 后端拦截，越权失败（H.2） |
| 17-16 | 检查 git 历史 | 无密钥/敏感信息入库（H.3/K.3） |
| 17-17 | 拔掉主模型 Provider 再发消息 | 自动降级到备用模型，不报错（I.2） |
| 17-18 | 新接入子系统前查选型表 | 先有现成方案清单可用，不自研（J 域） |
| 17-19 | 发布新版本 | 灰度→全量→可回滚，带监控告警（K.8/K.9） |
| 17-20 | 上线前评审 | 需求有明确 KPI、流程已梳理、有人机分工设计（L.1/L.3/L.6） |

---

## 18 可接入生态（9 类 ≈100 项，抽测 14 条）

| # | 步骤 | 预期结果 |
|---|------|----------|
| 18-1 | 建新模块前查选型清单 | 生态清单可查到候选（A-I 类齐全），有接入策略标注 |
| 18-2 | 查看 MCP 工具接入 | 平台工具层兼容 MCP 协议，可挂标准 MCP server（B.1/B.2） |
| 18-3 | 配置浏览器自动化工具 | 通过 browser-use 类工具执行网页任务成功（B.3/C.2） |
| 18-4 | 接本地模型 | 通过 Ollama/vLLM 接入开源模型并可对话（D.4/D.5） |
| 18-5 | 接多模型网关 | OpenRouter/OneAPI 类网关配置后可轮询路由（D.3/A.12） |
| 18-6 | 配向量库 | pgvector/Qdrant 任选其一，知识库检索可用（E.1/E.2） |
| 18-7 | 配全文检索 | 关键词检索（BM25）可用且与向量可混合（E.4） |
| 18-8 | 接 IM 渠道 | 企微/飞书/Telegram 任一渠道发布成功（G 类） |
| 18-9 | 接可观测平台 | Langfuse 类 trace 接入，对话链路可查（H.1） |
| 18-10 | 接评测框架 | Ragas/Promptfoo 类评测可运行（H.7） |
| 18-11 | 接入开放平台网关 | OneAPI 类网关密钥管理/计费可用（A.12） |
| 18-12 | 文档库对接 | 知识库接入 RAGFlow 类解析引擎后复杂 PDF 解析质量达标（A.4） |
| 18-13 | 新工具接入评估 | 走“灵魂三问”流程：有现成/差距/自研成本，有结论记录（17-J） |
| 18-14 | 生态清单时效性 | 清单含项目 star 量级与版本信息，可更新（I.6 等） |

---

## 19 布局与设计（7 域 ≈74 项，抽测 14 条）

| # | 步骤 | 预期结果 |
|---|------|----------|
| 19-1 | 打开任一管理页面 | 遵循标准骨架：Header+Sidebar+内容区（B.1） |
| 19-2 | 检查一级导航 | 按对象分类的 12 个一级模块齐全，命名一致（A.3/A.5） |
| 19-3 | 打开资源列表页 | 有状态 Tab+筛选+搜索+空状态（A.7/A.10/A.12） |
| 19-4 | 打开智能体详情页 | 左配置右测试布局，4-Tab 不丢状态（B.4/C.4） |
| 19-5 | 打开工作流编辑页 | 左节点库+中画布+右属性面板（B.5） |
| 19-6 | 打开对话页 | 左会话列表+右消息流+底部输入区（B.3/G.2） |
| 19-7 | 全局搜索定位 | Ctrl+K 命令面板可搜索资源并跳转（C.6） |
| 19-8 | 检查状态徽标 | 颜色体系全局一致（绿运行/红错误/黄草稿/蓝审核）（D.4） |
| 19-9 | 长列表页 | 超 500 条不卡顿（虚拟滚动生效）（G.1 域） |
| 19-10 | 切换暗色模式 | 一键切换且图表联动（E.7） |
| 19-11 | 缩窄浏览器到 768px | 移动端布局可用，抽屉导航生效（F.1/F.2） |
| 19-12 | 键盘走查 | Tab 顺序合理、Esc 关闭弹窗、焦点可见（F.3/F.5） |
| 19-13 | 检查 Dashboard | 指标卡+图表+任务待办+通知动态四区齐全（G.1） |
| 19-14 | 审查硬编码样式 | 颜色/间距全部走设计 token（E.10） |

---

## 20 底层基础能力（7 域 ≈66 项，抽测 14 条）

| # | 步骤 | 预期结果 |
|---|------|----------|
| 20-1 | 发起对话 | 首 token <1.5s，逐字流式渲染，非整段返回（A.1/A.4） |
| 20-2 | 长回复中点击停止 | 立即中断，已收内容保留可复制（A.5） |
| 20-3 | 测试区多模型对比 | 2-4 路并行流独立渲染（A.7） |
| 20-4 | 跑长任务（解析大 PDF） | 进度流显示阶段与百分比（A.8） |
| 20-5 | 模型输出 JSON | 100% 通过 schema 校验，非法自动修正（B.1/B.2/B.5） |
| 20-6 | 工具调用 | 走原生 function calling，调用过程流式可见（B.3/A.10） |
| 20-7 | 问知识库问题 | 关键词+向量混合检索，答案带引用溯源（D.3/D.8） |
| 20-8 | 知识库入库 | 文本经分词/清洗/去重/嵌入全管道（C.1/C.12） |
| 20-9 | 上传文本看标签 | 自动生成关键词/标签（top-5 带权重）（C.2） |
| 20-10 | 连续快速请求 | 触发限流返回 429+Retry-After（E.2） |
| 20-11 | 停掉主模型 Provider | 熔断后自动降级备用模型，有日志告警（E.3/E.11） |
| 20-12 | 检索延迟 | P95 <500ms（E.12） |
| 20-13 | 上传图片问内容 | 图像理解可答（F.5）；扫描件走 OCR（F.4） |
| 20-14 | 发语音 | ASR 转文字并进对话（F.2） |

---

## 21 文档与辅助体系（7 域 ≈56 项，抽测 14 条）

| # | 步骤 | 预期结果 |
|---|------|----------|
| 21-1 | 制造一个业务错误（非法配置） | 提示为人话+错误码+排查文档链接三段式（A.4） |
| 21-2 | 查错误码表 | docs/error-codes.md 有该码：消息模板/触发条件/排查指引（A.2/A.3） |
| 21-3 | 看前端错误提示 | 分级正确（toast/内联/横幅），同错误 30s 不重复（B.1/B.2） |
| 21-4 | 复制错误信息 | 弹窗可复制含错误码+时间的诊断信息（B.8） |
| 21-5 | 帮助中心搜“模型连不上” | 命中 FAQ 与错误排查手册（C.1/C.2/D.5） |
| 21-6 | 新用户打开快速开始 | 5 分钟步骤可完成首个智能体（D.1） |
| 21-7 | 查看某模块文档 | 用户手册含概念+操作+截图+FAQ（D.2） |
| 21-8 | 打开 API 文档 | 自动生成、可在线调试、含错误码说明（E.2） |
| 21-9 | 照部署文档部署 | 步骤可完成，含配置表/升级/回滚（E.5） |
| 21-10 | 查 changelog | Keep a Changelog 格式，版本化（E.10/D.9） |
| 21-11 | 管理员看手册 | 覆盖成员/权限/备份/安全（F.1/F.3/F.4） |
| 21-12 | CI 检查文档 | 链接 404 检测通过，必读文档存在（G.4） |
| 21-13 | 文档页反馈 | 每页有“是否有帮助”反馈入口（G.5） |
| 21-14 | 报错后看监控 | 错误码聚合统计可见该错误（A.8） |

---

## 22 数据治理与数据资产（抽测 14 条）

| # | 步骤 | 预期结果 |
|---|------|----------|
| 22-1 | 新建数据源（数据库/文件/API） | 探针扫描成功，资产出现在资产目录（A.1/D.1） |
| 22-2 | 资产目录搜索“客服” | 按关键词/分类/标签/负责人过滤命中（A.2） |
| 22-3 | 打开资产详情 | 含业务信息+技术元数据+质量评分+使用统计（B.1/B.2） |
| 22-4 | 查看资产血缘图 | 展示来源→流转→加工→产出的端到端链路（B.3） |
| 22-5 | 编辑业务元数据 | 保存后资产列表显示新名称/新标签（B.4） |
| 22-6 | 配置质量规则（如空值率>20%） | 定时扫描触发告警，质量问题明细可见（C.1/C.2） |
| 22-7 | 新建含手机号的资产 | 敏感识别自动标记分类=敏感（D.2） |
| 22-8 | 非负责人访问敏感资产 | 访问被拦截或触发审批（F.1） |
| 22-9 | 资产归档/销毁 | 状态流转正确，销毁后目录不再出现（E.2） |
| 22-10 | 通过数据服务 API 查资产 | 开放接口返回资产元数据且带权限校验（G.1） |
| 22-11 | 查看资产变更审计 | 记录 who/what/when 完整（F.3） |
| 22-12 | 对话日志自动登记为资产 | 新对话产生后资产目录出现对应资产条目（A.4） |
| 22-13 | 资产导出 | 触发合规审批流，审批后导出成功（F.4） |
| 22-14 | 查看资产使用统计 | 展示调用次数/最近访问/使用趋势（B.5） |

---

## 23 成本计费与配额治理（抽测 14 条）

| # | 步骤 | 预期结果 |
|---|------|----------|
| 23-1 | 完成一次多轮对话 | 成本看板出现该会话明细（模型/token/费用）（A.1） |
| 23-2 | 成本看板按模型下钻 | 各模型费用占比与趋势正确（A.2） |
| 23-3 | 修改价格表（覆盖默认价） | 新价格立即生效，后续计费按新价（B.1/B.2） |
| 23-4 | 创建预算（告警阈值 50/80/90/100%） | 用量达阈值触发对应级别告警（C.1） |
| 23-5 | 预算达 100% | 自动执行超限动作（降级/熔断），调用不报错而是降级（C.2） |
| 23-6 | 为某用户设置 token 配额 | 超额后按策略限速而非崩溃（D.1/D.2） |
| 23-7 | 命中 prompt 缓存 | 按 cache_read 低价计费，账单体现缓存项（E.2） |
| 23-8 | 使用比价器输入业务参数 | 输出多模型成本对比且与账单一致（F.1） |
| 23-9 | 自动出账 | 账单按周期生成，明细/总额/状态正确（G.1） |
| 23-10 | 按功能标签分摊成本 | 各部门/项目成本报表生成（H.1） |
| 23-11 | 查看成本优化建议 | 识别高消耗组件并给出替换/缓存/瘦身建议（I.1） |
| 23-12 | 调整预算 | 需审批，审批后生效并留审计（J.2） |
| 23-13 | 异常用量检测 | 突增/循环调用触发告警（J.3） |
| 23-14 | 成本数据跨组织查看 | 被隔离，仅本组织可见（J.1） |

---

## 24 测试与质量保障（抽测 14 条）

| # | 步骤 | 预期结果 |
|---|------|----------|
| 24-1 | 创建测试套件并批量导入用例 | 用例导入成功，可在编辑器查看/修改（A.1/A.2） |
| 24-2 | 将线上失败对话一键沉淀为用例 | 用例进入库并带会话上下文（A.4） |
| 24-3 | 手动运行套件 | 进度条+结果报告，失败项含输出/指标/错误（B.1/B.2） |
| 24-4 | 重跑失败用例 | 仅失败项执行，结果更新（B.3） |
| 24-5 | 查看回归趋势 | 历次运行指标趋势图+基线对比（C.1） |
| 24-6 | 故意改坏 Prompt 并提交 | CI 质量门禁拦截发布，报告指标下降（D.1） |
| 24-7 | 注入断网混沌场景 | 系统降级而非崩溃，恢复后自愈（E.1） |
| 24-8 | 注入模型超时场景 | 触发重试/降级策略，错误率可控（E.2） |
| 24-9 | 运行提示词攻击用例库 | 注入检测报告生成，高风险用例标红（F.1） |
| 24-10 | 查看 LLM 判分器打分 | 判分结果与人工标注一致性达阈值（B.4） |
| 24-11 | 设置质量门禁阈值 | 分低警告/拦截动作按配置生效（D.2） |
| 24-12 | 定时回归调度 | 到点自动运行并推送结果（C.2） |
| 24-13 | 篡改黄金样本 | 被审批拦截（防刷分）（G.2） |
| 24-14 | 生产流量抽样评估 | 抽样结果回流为用例（H.1） |

---

## 25 性能工程（抽测 14 条）

| # | 步骤 | 预期结果 |
|---|------|----------|
| 25-1 | 发起流式对话 | 性能看板记录 TTFT/TPOT（A.1） |
| 25-2 | 压测 100 并发 5 分钟 | 报告含 TTFT p50/p95/p99+吞吐+错误率（B.1） |
| 25-3 | 对比两次压测报告 | 指标对比图+SLO 达成率（C.1） |
| 25-4 | 重复提问相同前缀 | 前缀缓存命中率上升，TTFT 下降（D.1） |
| 25-5 | 查看缓存条目并清理 | 清理后命中率下降，可恢复（D.2） |
| 25-6 | 负载上升 | 自动伸缩按阈值扩副本，负载回落缩容（E.1） |
| 25-7 | 队列打满 | 返回 429 限流而非崩溃（F.1） |
| 25-8 | 查看队列监控 | 排队长度/等待时间/丢弃率可见（F.2） |
| 25-9 | 查看调优建议 | 自动识别瓶颈并给出建议（G.1） |
| 25-10 | 生产执行压测 | 被拦截，需审批（H.2） |
| 25-11 | 流式中断重连 | 断线恢复后从断点续传（A.3） |
| 25-12 | 设置请求超时 | 超时请求按策略重试/降级（F.3） |
| 25-13 | 开启动态批处理 | 吞吐提升且排队延迟可控（E.2） |
| 25-14 | 查看错误率分布 | 与监控告警联动（12-monitor）（A.4） |

---

## 26 用户研究与增长（抽测 14 条）

| # | 步骤 | 预期结果 |
|---|------|----------|
| 26-1 | 前端完成登录+对话 | 埋点明细出现对应事件（A.1） |
| 26-2 | 查看指标大盘 | 北极星+DAU/MAU/留存/激活率卡片与趋势正确（B.1） |
| 26-3 | 定义新埋点事件 | 参数 schema 校验生效，可灰度生效（A.2） |
| 26-4 | 查看次日/7 日/30 日留存 | 留存曲线正确，同期群可切换（C.1/C.2） |
| 26-5 | 创建漏斗（注册→首次对话→连续使用） | 各步骤转化率正确，定位流失断点（D.1） |
| 26-6 | 创建 A/B 实验（变体+流量分配） | 运行到期后给出显著性结论（E.1/E.2） |
| 26-7 | 查看实验经验库 | 往期实验结论可检索复用（E.3） |
| 26-8 | 规则分群（7 日活跃+付费） | 分群人数预览与导出正确（F.1） |
| 26-9 | 对话内点踩 | 反馈中心出现记录并可下钻会话（G.1） |
| 26-10 | 查看 NPS 看板 | 分数分布/趋势/评语明细可见（G.2） |
| 26-11 | 查看用户路径分析 | Aha 时刻候选动作被标记（H.1） |
| 26-12 | 埋点数据导出 | 含用户删除权支持（I.2） |
| 26-13 | 质量差会话回灌测试库 | 自动进入 24-测试 用例库（I.3） |
| 26-14 | 生产实验审批 | 非审批实验不可创建（J.3） |

---

## 汇总统计

| 模块 | 测试项数 |
|------|---------|
| 01 提示词 | 7 |
| 02 沙箱 | 6 |
| 03 上下文 | 5 |
| 04 工具 | 7 |
| 05 记忆 | 7 |
| 06 模型 | 6 |
| 07 工作流 | 10 |
| 08 语音 | 6 |
| 09 定时 | 7 |
| 10 技能插件 | 7 |
| 11 评估 | 6 |
| 12 监控 | 6 |
| 13 IAM | 7 |
| 14 生命周期 | 7 |
| 15 对话体验 | 20 |
| 16 企业级通用 | 30 |
| 17 教训与坑 | 20 |
| 18 可接入生态 | 14 |
| 19 布局与设计 | 14 |
| 20 底层基础能力 | 14 |
| 21 文档与辅助体系 | 14 |
| 22 数据治理与数据资产 | 14 |
| 23 成本计费与配额治理 | 14 |
| 24 测试与质量保障 | 14 |
| 25 性能工程 | 14 |
| 26 用户研究与增长 | 14 |
| **合计** | **290** |

> 全绿 = 功能完整性验收通过。任一条失败 = 对应模块存在真实缺口，按"缺口/补齐路径"修复后重跑。
