# FastAPI service
fastapi>=0.115.0
uvicorn[standard]>=0.52.1
pydantic>=2.13.4
pydantic-settings>=2.14.2

# L1+L2: LangChain core
langchain>=1.3.14
langchain-core>=0.3.0

# L1: LLM providers
langchain-openai>=0.2.0
langchain-anthropic>=1.5.4
langchain-ollama>=0.2.0

# L4: Agent framework (LangGraph v1.0 API)
langgraph>=1.2.10
langgraph-checkpoint-sqlite>=2.0.0
# langgraph-checkpoint-postgres>=2.0.0  # optional production persistence (M3.12)

# L5: Tool dependencies (incl. MCP client, M4.16)
httpx>=0.28.0

# L2: Token management
tiktoken>=0.13.0
jieba>=0.42.0          # 20-C: 中文分词与关键词提取
jsonschema>=4.0.0      # 20-B: 结构化输出 JSON Schema 严格校验

# L6: Memory / RAG (M5.9-M5.10)
# chromadb>=0.5.0            # optional vector store backend
# langchain-community>=0.3.0 # optional community embeddings
# pypdf>=4.0.0              # PDF ingestion (RAGEngine.ingest_file)
# python-docx>=1.1.0        # DOCX ingestion
# openpyxl>=3.1.0           # XLSX ingestion
# redis>=5.2.0              # optional distributed cache

# L7: A2A / orchestration (M6.15-M6.17)
# a2a-sdk>=0.2.0            # optional official A2A SDK

# L8: API extras
python-multipart>=0.0.32
pyyaml>=6.0.0           # M12: yaml agent import / M13: backup export

# M12: Voice (TTS/STT, M12.1-M12.6)
edge-tts>=6.1.0            # free TTS (no API key)
# faster-whisper>=1.0.0     # optional local STT (offline, STT_ENGINE=faster-whisper)
# soundfile>=0.12.0         # audio decode for local STT

# L4: Optional framework adapters (framework-agnostic, M0.21-M0.22)
# openai-agents>=0.1.0      # framework='openai-agents' (OpenAI Agents SDK)
# claude-agent-sdk>=1.0.0   # framework='claude-sdk' (Anthropic Agent SDK)
# google-adk>=0.1.0         # framework='adk' (Google ADK)
# pyautogen>=0.2.0          # framework='autogen' (AutoGen/AG2)

# L10: Observability (M13)
# opentelemetry-api>=1.26.0
# opentelemetry-sdk>=1.26.0
# opentelemetry-exporter-otlp-proto-http>=1.26.0
# prometheus-client>=0.20.0  # optional native Prometheus export

# M10: Evaluation (offline)
# pytest>=8.0.0
# pytest-asyncio>=0.23.0
# evaluate>=0.4.0           # HuggingFace evaluate

# Debugging
# langsmith>=0.1.0
